<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="stock_consume_request_list_view" model="ir.ui.view">
        <field name="name">stock.consume.request.list.view</field>
        <field name="model">stock.consume.request</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="warehouse_id"/>
                <field name="request_date"/>
                <field name="approve_date"/>
                <field name="state"/>
            </list>
        </field>
    </record>

    <record id="stock_consume_request_form_view" model="ir.ui.view">
        <field name="name">stock.consume.request.form.view</field>
        <field name="model">stock.consume.request</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_send_approve" string="Gửi duyệt" class="btn-primary" type="object" invisible="state != 'draft'"/>
                    <button name="action_approve" string="Duyệt" invisible="not approve_ok or state != 'pending'" type="object" class="btn-primary"/>
                    <button name="action_reset" string="Chỉnh sửa" invisible="state != 'pending'" type="object" class="btn_primary"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="picking_id" readonly="1"/>
                            <field name="request_user_id" readonly="1"/>
                            <field name="consume_type" readonly="state != 'draft'"/>
                            <field name="responsible_user_id" readonly="state != 'draft'"/>
                        </group>
                        <group>
                            <field name="request_date" readonly="1"/>
                            <field name="approve_date" readonly="1"/>
                            <field name="warehouse_id" readonly="state != 'draft'"/>
                            <field name="reason" readonly="state != 'draft'"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Chi tiết đề nghị xuất dùng">
                            <button name="action_import_product" string="Nhập sản phẩm" type="object" invisible="state != 'draft'"  class="btn btn-primary"/>
                            <field name="line_ids">
                                <list editable="bottom">
                                    <field name="product_code"/>
                                    <field name="product_id"/>
                                    <field name="uom_id"/>
                                    <field name="quantity"/>
                                </list>
                            </field>
                        </page>
                        <page string="Thông tin phê duyệt" name="approval_info">
                            <group>
                                <group>
                                    <field name="current_approve_user_ids" readonly="1" widget="many2many_tags"/>
                                    <field name="next_approve_user_ids" readonly="1" widget="many2many_tags"/>
                                    <field name="notif_user_ids" readonly="1" widget="many2many_tags"/>
                                </group>
                                <group groups="base.group_no_one">
                                    <field name="process_id" readonly="1" groups="base.group_no_one"/>
                                    <field name="date_sent" readonly="1" groups="base.group_no_one"/>
                                    <field name="date_approved" readonly="1" groups="base.group_no_one"/>
                                </group>
                            </group>
                            <field name="rule_line_ids"/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>
    <record id="action_stock_consume_request" model="ir.actions.act_window">
        <field name="name">Phiếu đề nghị xuất dùng</field>
        <field name="res_model">stock.consume.request</field>
        <field name="view_mode">list,form</field>
    </record>

    <record id="action_stock_consume_picking" model="ir.actions.act_window">
        <field name="name">Phiếu điều chuyển xuất dùng</field>
        <field name="res_model">stock.picking</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('consume_request_id', '!=', False)]</field>
    </record>
</odoo>
